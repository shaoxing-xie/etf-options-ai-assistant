# 工具参考手册（概述）

基于 OpenClaw 的 A股 / ETF 交易助手（含可选期权扩展能力），工具清单以 **config/tools_manifest.yaml** 为单一数据源，插件从 **config/tools_manifest.json**（由 `python scripts/generate_tools_json.py` 生成）加载并注册。

---

## 工具总数与分类

| 分类 | 数量 | 说明 |
|------|------|------|
| 数据采集 | 25 | 指数、ETF、期权、期货、个股、涨停、龙虎榜、资金流向、北向等 |
| 时段与编排 | 4 | A股时段、Copilot、可交易性过滤、事件哨兵 |
| 分析 | 16 | 技术指标、盘前/盘后/开盘分析、波动率、信号、风险评估、趋势、研究（Regime/轮动/策略）、**策略融合（`tool_strategy_engine`）** |
| 风险控制 | 5 | 仓位与硬锁定、止盈止损 |
| 策略效果跟踪 | 5 | 信号记录、表现、评分、权重 |
| 通知 | 5 | 飞书消息/卡片/信号/日报/风险预警 |
| 数据访问 | 6 | 从缓存读取指数/ETF/期权数据 |
| 回测 | 2 | 涨停回马枪回测、参数敏感性 |
| **合计** | **77** | 以 `config/tools_manifest.yaml` 为准，全部在 OpenClaw 中注册 |

> **说明**：上表分类数量为便于导航的概览；**权威工具列表与参数**以 **`config/tools_manifest.yaml`**（及生成的 `tools_manifest.json`）为准。

---

## 策略引擎与信号融合：`tool_strategy_engine`

- **用途**：在单路 `tool_generate_signals` 之外，对多路 Rule 输出（如 src 信号 + ETF 趋势）做 **统一契约**（`SignalCandidate`）与 **加权融合**（v1 / v1.1 / v1.2）。  
- **参数与返回值**：见 `config/tools_manifest.yaml` 中 `tool_strategy_engine`（含「何时优先调用」长描述）。  
- **架构与 OpenClaw 调度**：`docs/architecture/strategy_engine_and_signal_fusion.md`、`config/openclaw_strategy_engine.yaml`、`plugins/strategy_engine/README.md`。  
- **命令行试跑**（项目根目录、`PYTHONPATH` 已含 `plugins`）：`python tool_runner.py tool_strategy_engine underlying=510300 index_code=000300`

---

## 使用方式

1. **OpenClaw Agent**：在 Agent 配置中声明要调用的工具  
2. **OpenClaw 工作流**：在工作流中编排工具顺序与参数  
3. **Gateway API**：通过 OpenClaw Gateway 直接调用  

工具名以 `tool_` 前缀开头，参数为 JSON。

---

## 文档索引（渐进披露）

- **[工具参考手册-速查.md](工具参考手册-速查.md)**：按分类的工具 ID / 标签 / 一句话说明  
- **[工具参考手册-场景.md](工具参考手册-场景.md)**：使用场景示例、最佳实践、代码与时间格式  
- **[工具参考手册-研究涨停回测.md](工具参考手册-研究涨停回测.md)**：研究、选股、涨停回马枪、回测类工具说明  
- **[错误码说明.md](错误码说明.md)**：tool_runner 错误码表与可选执行日志  

参数与返回结构的权威定义见 **config/tools_manifest.yaml**；修改后请执行 `python scripts/generate_tools_json.py` 再重载插件。
